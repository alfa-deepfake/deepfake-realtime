# Pinned to what actually runs on the A100 box.
#
# NOT seed-vc's own requirements.txt: that file's first lines pull torch from
# the cu126 *nightly* index, which then conflicts with the pinned torch==2.4.0
# further down. These are the versions the working venv ended up with.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.4.0+cu124
torchaudio==2.4.0+cu124
torchvision==0.19.0+cu124

# numpy must stay <2 — numba/librosa in these versions are built against 1.x.
numpy==1.26.4
scipy==1.13.1
librosa==0.10.2
soundfile==0.12.1
soxr==1.1.0

transformers==4.46.3
huggingface_hub==0.36.2
descript-audio-codec==1.0.0
munch==4.0.0
einops==0.8.0
hydra-core==1.3.2
omegaconf==2.3.0
pyyaml==6.0.3

# real-time-gui.py imports these at module level.
python-dotenv==1.2.2
tqdm==4.68.4

aiohttp==3.14.1

# onnxruntime is deliberately absent. seed-vc only imports it lazily inside
# modules/rmvpe.py (the ONNX RMVPE path, unused here) and in baselines/. Adding
# it makes the install unsolvable: onnxruntime needs protobuf>=4.25, while
# descript-audiotools pins protobuf<3.20.
